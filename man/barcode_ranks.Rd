% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-h5.R
\name{barcode_ranks}
\alias{barcode_ranks}
\title{Barcode-rank curve and knee/inflection estimates for a run}
\usage{
barcode_ranks(dir, feature_type = "Gene Expression")
}
\arguments{
\item{dir}{Path to a run directory (root or its \verb{outs/}).}

\item{feature_type}{Feature type used for the UMI totals. Defaults to
\code{"Gene Expression"}.}
}
\value{
A list with \code{ranks} (a tibble of \code{rank}, \code{total_umi} for the
deduplicated curve), \code{knee} and \code{inflection} (estimated UMI thresholds),
and \code{n_barcodes_above_knee}.
}
\description{
Reads the raw (unfiltered) feature-barcode matrix, ranks barcodes by total
UMI count, and estimates the knee and inflection points on the log-log
barcode-rank curve. This is a lightweight sanity check on cell calling; it
does not reproduce the exact \code{cellranger}/\code{emptyDrops} algorithm.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ambient.R
\name{ambient_fraction}
\alias{ambient_fraction}
\title{Ambient (background) RNA fraction per GEM well}
\usage{
ambient_fraction(dirs, ids = NULL, feature_type = "Gene Expression")
}
\arguments{
\item{dirs}{Character vector of run directories.}

\item{ids}{Optional run identifiers, same length as \code{dirs}.}

\item{feature_type}{Feature type to use. Defaults to \code{"Gene Expression"}.}
}
\value{
A tibble with one row per run: \code{run_id}, \code{total_umi} (raw),
\code{cell_umi}, \code{pct_ambient}, \code{n_empty}, \code{mean_umi_per_empty}.
}
\description{
Estimates how much of the sequenced signal comes from cell-free ("empty")
droplets: reads the raw (unfiltered) matrix and the set of called-cell
barcodes, then reports the fraction of total UMIs that fall outside cells. A
high value flags ambient/soup contamination.
}

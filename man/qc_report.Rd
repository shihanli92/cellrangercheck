% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{qc_report}
\alias{qc_report}
\title{Render a side-by-side QC report across GEM wells}
\usage{
qc_report(
  dirs,
  output_file,
  ids = NULL,
  thresholds = default_thresholds(),
  include_h5 = TRUE,
  fastqc = NULL,
  progress = interactive()
)
}
\arguments{
\item{dirs}{Character vector of run directories.}

\item{output_file}{Path for the rendered HTML file.}

\item{ids}{Optional run identifiers, same length as \code{dirs}.}

\item{thresholds}{Threshold tibble; see \code{\link[=default_thresholds]{default_thresholds()}}.}

\item{include_h5}{Whether to compute h5-derived metrics (barcode-rank knee,
UMI/gene distributions). Set \code{FALSE} to skip if matrices are absent.}

\item{fastqc}{Optional character vector of directories to scan for FastQC
outputs. When supplied, a FastQC section (per-module PASS/WARN/FAIL grid and
basic statistics, grouped by reaction) is appended. FastQC files are matched
to reactions via each run's \code{config.csv} fastq prefixes.}

\item{progress}{Whether to print timed progress messages while building the
report. Defaults to \code{TRUE} in interactive sessions.}
}
\value{
The path to the rendered report, invisibly.
}
\description{
Parses each run, flags metrics, and renders an HTML report comparing all GEM
reactions. Requires the suggested packages \code{rmarkdown}, \code{knitr}, \code{ggplot2}
and \code{DT}.
}
\details{
The metrics (including the slower h5 reads) are computed up front so progress
can be reported; the Rmd template only renders the precomputed results.
}

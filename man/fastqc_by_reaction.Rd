% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastqc.R
\name{fastqc_by_reaction}
\alias{fastqc_by_reaction}
\title{Parse FastQC outputs and attribute them to Cell Ranger reactions}
\usage{
fastqc_by_reaction(fastqc_dirs, run_dirs, ids = NULL)
}
\arguments{
\item{fastqc_dirs}{Character vector of directories to scan for FastQC output.}

\item{run_dirs}{Character vector of Cell Ranger run directories.}

\item{ids}{Optional run identifiers, same length as \code{run_dirs}.}
}
\value{
A list with \code{stats} and \code{modules} tibbles (as in \code{\link[=parse_fastqc]{parse_fastqc()}}),
each gaining \code{run_id}, \code{fastq_id} and \code{feature_types} columns. Files that
match no declared fastq_id keep \code{NA} in those columns (with a warning).
Returns \code{NULL} if no FastQC outputs are found.
}
\description{
Scans \code{fastqc_dirs} for FastQC outputs, reads each run's \code{outs/config.csv}
to learn its declared fastq prefixes, and joins the two so every FastQC file
is tagged with the GEM reaction (run) and library it belongs to.
}

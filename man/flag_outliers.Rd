% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{flag_outliers}
\alias{flag_outliers}
\title{Flag cross-well metric outliers}
\usage{
flag_outliers(wide, nmads = 3, metrics = NULL)
}
\arguments{
\item{wide}{A wide metrics table from \code{\link[=metrics_wide]{metrics_wide()}}.}

\item{nmads}{Threshold in MADs. Defaults to 3.}

\item{metrics}{Optional character vector of metric columns to consider;
defaults to all numeric columns.}
}
\value{
A long tibble of the flagged values: \code{run_id}, \code{sample_id}, \code{metric},
\code{value}, \code{median}, \code{mad}, \code{n_mads}, \code{is_outlier}. Columns with fewer than 3
non-missing wells or zero MAD are skipped (no robust call possible). Returns
zero rows when nothing is flagged.
}
\description{
Across GEM wells, flags values that deviate from the cohort by more than
\code{nmads} median absolute deviations (a robust z-score). Useful for spotting
the one well that is off on cells recovered, saturation, mito content, etc.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{metrics_wide}
\alias{metrics_wide}
\title{Pivot the tidy metrics table to one comparable row per sample}
\usage{
metrics_wide(long, category = "Cells")
}
\arguments{
\item{long}{A tidy tibble from \code{\link[=parse_cellranger]{parse_cellranger()}} /
\code{\link[=parse_cellranger_runs]{parse_cellranger_runs()}}.}

\item{category}{Which \code{category} rows to include. Rows with a missing
category (standalone count runs) are always included.}
}
\value{
A wide tibble keyed by \code{run_id}, \code{sample_id}, \code{pipeline} with numeric
metric columns named \verb{<library_type>__<metric>}.
}
\description{
Builds the side-by-side comparison table: one row per (run, sample) with a
column per \code{library_type} x \code{metric}. By default only the per-sample cell
metrics (\code{Category == "Cells"}, plus standalone-count rows which have no
category) are used, since those describe the biology being compared across
GEM wells. Library-level sequencing metrics remain available in the long
table.
}
\examples{
\dontrun{
metrics_wide(parse_cellranger_runs(c("sample0", "sample1")))
}
}

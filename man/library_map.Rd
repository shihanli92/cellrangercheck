% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{library_map}
\alias{library_map}
\title{Build a fastq_id -> reaction map from run config.csv files}
\usage{
library_map(dirs, ids = NULL)
}
\arguments{
\item{dirs}{Character vector of run directories.}

\item{ids}{Optional run identifiers, same length as \code{dirs}.}
}
\value{
A tibble with \code{run_id}, \code{fastq_id}, \code{fastqs}, \code{feature_types}. Runs
without a readable \code{config.csv} contribute no rows (with a warning).
}
\description{
Reads the \verb{[libraries]} section of each run's \code{outs/config.csv} and returns
the fastq prefixes declared for that run, so FastQC outputs (named by fastq
prefix) can be attributed to the GEM reaction that produced them.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-qc.R
\name{cell_qc}
\alias{cell_qc}
\title{Per-cell QC summary per GEM well}
\usage{
cell_qc(
  dirs,
  ids = NULL,
  min_genes = 200,
  max_pct_mito = 20,
  feature_type = "Gene Expression",
  mito_pattern = "^mt-",
  ribo_pattern = "^rp[sl]"
)
}
\arguments{
\item{dirs}{Character vector of run directories.}

\item{ids}{Optional run identifiers, same length as \code{dirs}.}

\item{min_genes, max_pct_mito}{Filters defining a "usable" cell: at least
\code{min_genes} detected genes and at most \code{max_pct_mito} percent mitochondrial.}

\item{feature_type}{Feature type to use. Defaults to \code{"Gene Expression"}.}

\item{mito_pattern, ribo_pattern}{Gene-symbol regexes (case-insensitive).}
}
\value{
A tibble with one row per (run, sample): \code{run_id}, \code{sample_id},
\code{n_cells}, \code{median_umi}, \code{median_genes}, \code{median_pct_mito}, \code{mad_pct_mito},
\code{pct_cells_high_mito}, \code{median_pct_ribo}, \code{median_complexity},
\code{n_cells_pass}, \code{pct_cells_pass}.
}
\description{
Computes mitochondrial and ribosomal content, library complexity and a
cell-quality pass-rate from each run's filtered (cell) matrix. Mitochondrial
and ribosomal genes are matched on gene symbol, case-insensitively, so the
defaults work for both mouse (\verb{mt-}, \code{Rps}/\code{Rpl}) and human (\verb{MT-},
\code{RPS}/\code{RPL}).
}

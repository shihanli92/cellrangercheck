% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{default_thresholds}
\alias{default_thresholds}
\title{Default QC thresholds for Cell Ranger metrics}
\usage{
default_thresholds()
}
\value{
A tibble of thresholds with columns \code{library_type}, \code{category},
\code{grouped_by}, \code{metric}, \code{direction}, \code{warn}, \code{fail}.
}
\description{
A starting set of pass/warn/fail thresholds keyed by library type and metric.
\code{direction} is \code{"min"} when higher values are better (value below \code{warn}
warns, below \code{fail} fails) or \code{"max"} when lower is better. Percentage
metrics are on the 0-100 scale, matching \code{\link[=parse_cellranger]{parse_cellranger()}}.
}
\details{
Edit the returned tibble (add, drop or retune rows) and pass it to
\code{\link[=flag_qc]{flag_qc()}} to customise. \code{category}/\code{grouped_by} disambiguate which row of
the long metrics table a threshold applies to; \code{NA} in \code{grouped_by} matches
the per-sample \code{Cells} metrics.
}
\examples{
default_thresholds()
}
